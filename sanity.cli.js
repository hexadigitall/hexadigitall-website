/**
 * This configuration file lets you run `$ sanity [command]` in this folder
 * Go to https://www.sanity.io/docs/cli for more information.
 */
const sanityConfig = {
  api: {
    projectId: 'puzezel0',
    dataset: 'production'
  }
}

export default sanityConfig;
